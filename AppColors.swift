import UIKit

struct AppColors {
    
    // 예시 색상들 (여기에 원하는 만큼 추가)
    static let mainBackground = UIColor(hex: "#0AAAAAAA")  // 너가 준 색상
    static let lightblue = UIColor(hex: "#2196F3")  // 너가 준 색상
    
 
}
